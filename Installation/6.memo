1.update 产品的时候，修改了所有的字段之后，然后删除图片的时候，其他所有修改了的字段的值都保存了，但是小分类的下拉列表被重新刷新了，没有保存删除图片之前
的值
2.图片表里边的valid flg目前没有作用
3.产品图片，用户删掉的，并没有实际删掉，还是保存在server上的
4.detail和update产品的页面，修改了之后能适应IE和chrome，其他弹出页面的，比如user，feedback这些没改动
5.detail弹出页面

Emall
1.buy和quote的时候，最大可选数量为9999999
2.目前利润点没有生效和失效日期，所以后边如果rate发生变化了，可能看不到之前rate是多少了。
3.目前积分的计算方法是，完成一单之后，用户和供应商都能积单子总价的%1,在完成订单的时候做的
4.数据提交的时候，应该多用try，catch
5.记录日志